# Agent Configuration

This directory is the checked-in source of truth for personal agent behavior on a machine.
Chezmoi applies it to `~/.agents`.

## Layout

- `skills/` maps to `~/.agents/skills` for personal Codex skills.
- `docs/` maps to `~/.agents/docs` for centralized agent-readable documentation.

## Codex Skills

- Prefer personal skills under `~/.agents/skills/<skill-name>/SKILL.md`.
- Invoke personal skills as `$skill-name` or select them with `/skills`.
- Keep built-in Codex slash commands separate from personal skills. For example, `/memories` configures Codex memory behavior, while `$memorise` is planned as a personal documentation-capture skill.
- Use `$skill-creator` when creating or revising skills.

## Documentation

- Use `~/.agents/docs` for durable machine-level documentation that should be available across projects.
- Keep project-specific notes in the project or in `~/notes` unless they describe default agent behavior.

## Canonical Task Tracking

- The machine-wide `~/.agents/tasks/todo.txt`, `done.txt`, and `handoffs/` directory are the durable task source of truth for every repository. Run the managed `todo` wrapper from any working directory for every read and write; never use raw redirects or bare `tuxedo`. The wrapper pins Tuxedo to that shared store; persistent Tuxedo views and reads are lock-free, while short mutations are serialized.
- At the start of nontrivial change work, resolve `repo_root="$(git rev-parse --show-toplevel 2>/dev/null || pwd)"` and inspect only its open tasks with `todo ls "repo:$repo_root" --json | jq '[.[] | select(.done == false)]'`. Do not load the unfiltered machine-wide queue into a project context unless the user explicitly asks for a cross-repository view. Reuse an open task only when it covers the same outcome and repository, or add one line per distinct deliverable using a priority, creation date, `+project`, `@agent`, stable `id:<uuid>`, `owner:<agent>`, and absolute `repo:<path>`. Do not create entries for trivial read-only questions.
- Prefer stable-ID mutations: `todo task-status ID STATUS`, `todo handoff get ID`, and, after validation succeeds, `todo task-done ID`. Never archive automatically.
- Keep blocked tasks open with `status:blocked` and a concise failure handoff. Do not modify unrelated tasks.
- Treat a user request beginning with `@todo` as an invocation of the global `$todo` skill. It creates repo-targeted tasks under one `flow:`, infers the dependency DAG, delegates ready work when the harness supports agents, and copies each dependency's durable handoff into downstream prompts.
- Ephemeral plans may support execution, but they never replace, duplicate, or split from the canonical todo.txt state.
- When an app is made accessible through tmux, launch it in the same working directory as the session's existing windows unless the user explicitly requests another root.

## External Utility Ownership

- Reusable utility implementations live in the pinned `kittentts-cli`, `tuxedo-project-todo`, `macos-default-apps`, `gh-create-repo`, and `unescape-cli` GitHub projects under `~/projects`.
- Their dotfiles modules own only manifests, catalog metadata, conditional `~/bin` links, and parent integration tests. The upstream projects own behavior, releases, packaging, and standalone install/uninstall lifecycle.
- Disabling a utility module or uninstalling these dotfiles removes the ChezMoi-owned links and preserves the project checkout. Do not duplicate an upstream implementation in the dotfiles source or run its standalone installer from bootstrap.

{{- if .modules.agentTimer.enabled }}

{{ includeTemplate "../modules/agent-timer/policy/time-boxed-delivery.md" . }}
{{- end }}
