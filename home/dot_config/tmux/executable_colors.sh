#!/usr/bin/env bash
# tmux colors - sourced from main colorscheme

# colors.sh is managed as a symlink to the selected colorscheme.
# The colors are exported as environment variables for tmux to use

source "$(dirname "$0")/../colorschemes/colors.sh"
