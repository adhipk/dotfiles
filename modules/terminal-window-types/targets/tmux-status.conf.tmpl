# Resolve the foreground process from each pane's TTY. This unwraps generic
# launchers such as node, bun, or Python without consulting the typed window
# metadata or its initial command.
set -g @dotfiles_window_label '#("$HOME/bin/tmux-window-program" #{pane_tty})'
