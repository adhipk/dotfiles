# Managed user preferences rendered from chezmoi data.
# Change values with `dotfiles-settings`; DOTFILES_PREF_<UPPERCASE_KEY>
# environment variables may override individual values for one session.
schema_version = 1
terminal_editor = "{{ .preferences.terminalEditor }}"
gui_editor = "{{ .preferences.guiEditor }}"
assistant = "{{ .preferences.assistant }}"
terminal_app = "{{ .preferences.terminalApp }}"
