{
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ -x \"$HOME/bin/agent-timer\" ]; then if output=$(\"$HOME/bin/agent-timer\" hook 2>/dev/null); then printf '%s\\n' \"$output\"; else printf '{}\\n'; fi; else cat >/dev/null; printf '{}\\n'; fi",
            "timeout": 5
          }
        ]
      }
    ],
    "SubagentStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ -x \"$HOME/bin/agent-timer\" ]; then if output=$(\"$HOME/bin/agent-timer\" hook 2>/dev/null); then printf '%s\\n' \"$output\"; else printf '{}\\n'; fi; else cat >/dev/null; printf '{}\\n'; fi",
            "timeout": 5
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ -x \"$HOME/bin/agent-timer\" ]; then if output=$(\"$HOME/bin/agent-timer\" hook 2>/dev/null); then printf '%s\\n' \"$output\"; else printf '{}\\n'; fi; else cat >/dev/null; printf '{}\\n'; fi",
            "timeout": 5
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ -x \"$HOME/bin/agent-timer\" ]; then if output=$(\"$HOME/bin/agent-timer\" hook 2>/dev/null); then printf '%s\\n' \"$output\"; else printf '{}\\n'; fi; else cat >/dev/null; printf '{}\\n'; fi",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
