# Durable agent-session inventory backed by sesh, with timer lifecycle actions.
bind-key -N "Manage agent timers and durable sessions" T display-popup -EE -w 90% -h 80% -d "#{pane_current_path}" -T "sesh · agent timers" "$HOME/bin/agent-timer manage"
